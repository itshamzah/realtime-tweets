<h1><small>@<%= current_user.nick_name %></small></h1><br />
<div class="container tweet-container">
	<h1>Wall</h1>

	<ul id="tweet-list" class="tweet-list">
		<% @tweets.each do |tweet| %>
			<%= render :partial => "dashboard/tweet", :locals => { :tweet => tweet } %>
		<% end %>
	</ul>
</div>